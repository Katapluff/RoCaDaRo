// export const authMiddleware = (req, res, next) => {
//   // Überprüfen, ob der Benutzer angemeldet ist
//   const authToken = req.cookies.authToken;
//   if (authToken) {
//     // Benutzer ist angemeldet, setze den Benutzer im Request-Objekt
//     req.user = { authToken }; // Beispielhaftes Benutzerobjekt
//   }

//   next();
// };
